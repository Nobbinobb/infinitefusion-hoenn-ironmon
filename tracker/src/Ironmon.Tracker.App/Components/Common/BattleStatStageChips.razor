@foreach ((string Name, int Stage, bool UsesAccuracyFormula) stage in GetVisibleStages())
{
    <span class="battle-stat-chip" title="@BattleStatStageFormatter.FormatTooltip(stage.Stage, stage.UsesAccuracyFormula)">@stage.Name@BattleStatStageFormatter.FormatSignedStage(stage.Stage)</span>
}
