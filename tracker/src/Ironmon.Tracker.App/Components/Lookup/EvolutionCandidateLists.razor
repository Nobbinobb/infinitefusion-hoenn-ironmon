<div class="evolution-candidate-lists">
    @if (HasNormal)
    {
        <EvolutionCandidateList SpeciesId="@SpeciesId" Side="EvolutionCandidateSide.Normal" Heading="@Text["Redesign.Research.EvolutionCandidates"].Value" AriaLabel="@Text["Lookup.Card.EvolutionCandidates"].Value" Recipe="@Recipe" DebugMode="@DebugMode" DebugTarget="@DebugTarget" DebugEnemyPosition="@DebugEnemyPosition" GameRoot="@GameRoot" ShowGraphAction="@ShowGraphAction" GraphOpened="@GraphOpened" Selected="@Selected" />
    }
    @if (HasHead || HasBody)
    {
        <section class="fusion-evolution-candidates" aria-label="@Text["Lookup.Card.EvolutionCandidates"]">
            @if (HasHead && HasBody)
            {
                <nav class="evolution-candidate-side-tabs" aria-label="@Text["Lookup.Card.EvolutionCandidates"]" role="tablist">
                    <button type="button" role="tab" aria-selected="@(_selectedFusionSide == EvolutionCandidateSide.Head ? "true" : "false")" class="@GetSideTabClass(EvolutionCandidateSide.Head)" @onclick="() => SelectFusionSide(EvolutionCandidateSide.Head)">@Text["Redesign.Research.HeadCandidates"]</button>
                    <button type="button" role="tab" aria-selected="@(_selectedFusionSide == EvolutionCandidateSide.Body ? "true" : "false")" class="@GetSideTabClass(EvolutionCandidateSide.Body)" @onclick="() => SelectFusionSide(EvolutionCandidateSide.Body)">@Text["Redesign.Research.BodyCandidates"]</button>
                </nav>
            }
            @if (_selectedFusionSide == EvolutionCandidateSide.Head && HasHead)
            {
                <EvolutionCandidateList SpeciesId="@SpeciesId" Side="EvolutionCandidateSide.Head" AlwaysShowFilter="true" Heading="@Text["Redesign.Research.HeadEvolutions"].Value" AriaLabel="@Text["Lookup.Card.HeadEvolutionTargets"].Value" Recipe="@Recipe" DebugMode="@DebugMode" DebugTarget="@DebugTarget" DebugEnemyPosition="@DebugEnemyPosition" GameRoot="@GameRoot" ShowGraphAction="@ShowGraphAction" GraphOpened="@GraphOpened" Selected="@Selected" />
            }
            else if (HasBody)
            {
                <EvolutionCandidateList SpeciesId="@SpeciesId" Side="EvolutionCandidateSide.Body" AlwaysShowFilter="true" Heading="@Text["Redesign.Research.BodyEvolutions"].Value" AriaLabel="@Text["Lookup.Card.BodyEvolutionTargets"].Value" Recipe="@Recipe" DebugMode="@DebugMode" DebugTarget="@DebugTarget" DebugEnemyPosition="@DebugEnemyPosition" GameRoot="@GameRoot" ShowGraphAction="@ShowGraphAction" GraphOpened="@GraphOpened" Selected="@Selected" />
            }
        </section>
    }
</div>
